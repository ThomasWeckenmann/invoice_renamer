/** Root component of the desktop app; placeholder until the batch workspace lands. */

export function App() {
  return (
    <main>
      <h1>Invoice Renamer</h1>
      <p>Desktop shell placeholder — batch workspace lands in a later milestone.</p>
    </main>
  );
}
